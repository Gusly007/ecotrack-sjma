{
  "name": "ecotrack-database",
  "version": "1.0.0",
  "description": "Gestion de la base de données EcoTrack (migrations, seeds)",
  "type": "module",
  "scripts": {
    "migrate": "cross-env NODE_OPTIONS=\"-r dotenv/config\" dotenv_config_path=../.env node-pg-migrate up -f migrate.config.cjs",
    "migrate:down": "cross-env NODE_OPTIONS=\"-r dotenv/config\" dotenv_config_path=../.env node-pg-migrate down -f migrate.config.cjs",
    "migrate:redo": "cross-env NODE_OPTIONS=\"-r dotenv/config\" dotenv_config_path=../.env node-pg-migrate redo -f migrate.config.cjs",
    "migrate:status": "cross-env NODE_OPTIONS=\"-r dotenv/config\" dotenv_config_path=../.env node-pg-migrate status -f migrate.config.cjs",
    "migrate:create": "cross-env NODE_OPTIONS=\"-r dotenv/config\" dotenv_config_path=../.env node-pg-migrate create -f migrate.config.cjs",
    "seed": "node scripts/seed.mjs",
    "seed:fresh": "node scripts/seed.mjs --fresh",
    "db:fresh": "node scripts/fresh.mjs",
    "db:reset": "node scripts/reset.mjs",
    "db:status": "node scripts/status.mjs",
    "fresh": "npm run db:fresh && npm run migrate && npm run seed",
    "reset": "npm run db:reset",
    "benchmark": "node scripts/benchmark.mjs",
    "benchmark:full": "node scripts/benchmark.mjs --full",
    "benchmark:avant": "node scripts/benchmark.mjs --avant",
    "benchmark:apres": "node scripts/benchmark.mjs --apres",
    "benchmark:seed": "node scripts/benchmark.mjs --seed-only",
    "benchmark:optim": "node scripts/benchmark.mjs --optim-only",
    "seed:prod": "node scripts/seed.mjs --fresh && node scripts/benchmark.mjs --seed-only",
    "backup": "node scripts/backup.mjs",
    "backup:dry": "node scripts/backup.mjs --dry-run",
    "backup:list": "node scripts/backup.mjs --list",
    "backup:verify": "node scripts/backup.mjs --verify",
    "backup:retention": "node scripts/backup.mjs --retention 14"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "node-pg-migrate": "^7.9.1",
    "pg": "^8.11.0",
    "pino": "10.1.1",
    "pino-pretty": "^11.3.0"
  },
  "keywords": [
    "database",
    "migrations",
    "postgresql",
    "ecotrack"
  ],
  "author": "EcoTrack Team",
  "license": "MIT",
  "overrides": {
    "glob@<=11.1.0": "11.1.0",
    "@isaacs/brace-expansion@<=5.0.1": "5.0.1"
  }
}
